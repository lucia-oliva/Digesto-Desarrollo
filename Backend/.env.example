# Copiar este archivo a `.env` si se ejecuta el backend fuera de Docker.
# En el entorno Docker las variables provienen del `.env` de la raíz.

DB_HOST=mariadb
DB_USER=digesto
DB_PASS=digesto
DB_NAME=bs_digesto

NODE_ENV=development
PORT=3000
FRONT_ORIGINS=http://localhost:5173

ACCESS_SECRET=cambiar-access-secret
REFRESH_SECRET=cambiar-refresh-secret

MAIL_USER=
MAIL_PASS=